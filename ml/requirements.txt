# Pinned toolchain for the bootstrap ONNX classifier (story 04.01) and the scheduled
# retrain candidate (story 10.02). These versions produced the checked-in
# src/main/resources/models/*.onnx and the parity fixtures; the Java side serves them
# with ONNX Runtime 1.26.x. The retrain pipeline (ml/train_candidate.py) trains and
# calibrates on this same toolchain so a candidate is byte-reproducible across machines.
scikit-learn==1.9.0
skl2onnx==1.20.0
onnxruntime==1.27.0
numpy==2.5.0
# requests: the retrain workflow's export fetcher (ml/fetch_training_data.py) pulls the
# labeled export + per-email features over HTTP from the live API.
requests==2.34.2
