# Keep the build context small — but DO keep .git so bootBuildInfo can read the
# commit for the /info endpoint.
.gradle/
build/
.idea/
*.iml
node_modules/
.next/
out/
# The console is its own deployable (Vercel / console/Dockerfile) — not part of
# the Java API image.
console/

# Local-only working context (not needed in the image).
planning/
CLAUDE.md
.claude/

# OS / editor noise
.DS_Store
*.swp
