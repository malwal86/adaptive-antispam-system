# Base URL of the Java API the console talks to. Must be an origin the API
# CORS-allows (see antispam.console.allowed-origins on the Spring side).
# Local dev default (when unset) is http://localhost:8080.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
